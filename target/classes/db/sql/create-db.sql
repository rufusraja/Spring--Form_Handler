--DROP TABLE users IF EXISTS;

CREATE TABLE users (
  id INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1, INCREMENT BY 1) PRIMARY KEY,
  activity VARCHAR(300),
  start_planned  VARCHAR(150),
  start_actual VARCHAR(100),
  finish_planned VARCHAR(100),
  finish_actual VARCHAR(100),
  status VARCHAR(200),

);